export class SelectorOption {
  readonly indicatorColor: string;
  readonly text: string;

  constructor(indicatorColor: string, text: string) {
    this.indicatorColor = indicatorColor;
    this.text = text;
  }
}
